enum FormType { signIn, register }
enum AuthenticationStatus { unknown, authenticated, unauthenticated }
enum EmailValidationError { invalid }
enum PasswordValidationError { length, oneDigit, specialCharacter }
enum LengthValidationError { length }
enum DogetagAvailability { unknown, loading, available, unavailable }
enum DobValidationError { month, day, year }
